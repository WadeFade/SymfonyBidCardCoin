{% extends 'base.html.twig' %}

{% block title %}Modifier Photo{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4">Modifier Photo</h1>

        {{ include('photo/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-primary mt-2" href="{{ path('photo_index') }}">Retour</a>

        {{ include('photo/_delete_form.html.twig') }}
    </div>
{% endblock %}
