{% if is_granted('IS_AUTHENTICATED_FULLY') %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="{{ path('home') }}"><i class="fas fa-gavel"></i></a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="#">Enchères</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('user_produit') }}">Vos produits</a>
                </li>
                {% if is_granted('ROLE_SUPER_ADMIN') or is_granted('ROLE_ADMIN') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('dashboard') }}">Dashboard Admin</a>
                    </li>
                {% endif %}
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item active">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        {# <div class="text-white">{% for role in app.user.roles %}{{ role }} {% endfor %}: {{ app.user.username }}</div> #}
    </nav>
{% endif %}
