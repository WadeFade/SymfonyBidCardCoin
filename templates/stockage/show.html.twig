{% extends 'base.html.twig' %}

{% block title %}Stockage{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4">Stockage</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stockage.id }}</td>
            </tr>
            </tbody>
        </table>

        <a class="btn btn-primary" href="{{ path('stockage_index') }}">Retour</a>

        <a class="btn btn-success" href="{{ path('stockage_edit', {'id': stockage.id}) }}">Modifier</a>

        {{ include('stockage/_delete_form.html.twig') }}
    </div>
{% endblock %}
