<form method="post" action="{{ path('salle_enchere_delete', {'id': salle_enchere.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ salle_enchere.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
