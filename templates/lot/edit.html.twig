{% extends 'base.html.twig' %}

{% block title %}Modifier Lot{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4">Modifier Lot</h1>

        {{ include('lot/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-primary" href="{{ path('lot_index') }}">Retour</a>

        {{ include('lot/_delete_form.html.twig') }}
    </div>
{% endblock %}
