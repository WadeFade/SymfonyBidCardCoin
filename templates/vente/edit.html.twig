{% extends 'base.html.twig' %}

{% block title %}Modifier Vente{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-4 mb-4">Modifier la Vente</h1>

        {{ include('vente/_form.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-primary mt-2" href="{{ path('vente_index') }}">Retour</a>

        {{ include('vente/_delete_form.html.twig') }}
    </div>
{% endblock %}
